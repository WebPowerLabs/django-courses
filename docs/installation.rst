============
Installation
============

At the command line::

    $ easy_install django-courses

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-courses
    $ pip install django-courses